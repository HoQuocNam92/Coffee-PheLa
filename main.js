const slides = document.querySelector('.hero-section');
let index = 0;

// setInterval(() => {
//     console.log("CHECK INDEX" , index)
//   index = (index + 1) % 3; // 3 là số lượng ảnh
//   slides.style.transform = `translateX(-${index * 100}%)`;
// }, 3000);
